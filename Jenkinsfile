pipeline {
  agent any
  stages {
    stage('Iniciando') {
      steps {
        echo 'Abrindo arquivo'
      }
    }

    stage('') {
      steps {
        mail(body: 'Estamos iniciando a pipeline', subject: '[Jenkins] iniciando pipeline')
      }
    }

  }
}